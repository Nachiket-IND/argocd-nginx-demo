# base/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: default   # ✅ Required for ArgoCD
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80

